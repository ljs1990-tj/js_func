# 🌐 실전 비동기 처리: Promise와 Async/Await 완벽 가이드

앞서 햄버거 만들기 비유를 통해 비동기 처리의 흐름을 알아보았습니다. 이제 진짜 실전으로 들어갈 차례입니다! 
웹 개발을 하다 보면 다른 서버(인터넷)에서 날씨 정보, 유저 목록 등의 **데이터를 가져와야 하는 상황**이 무조건 발생합니다. 이때 가장 많이 사용하는 자바스크립트의 내장 기능이 바로 **`fetch`** 입니다.

`fetch` 함수는 결과를 즉시 주지 않고 **`Promise`** 를 돌려줍니다. 이를 어떻게 다루는지 두 가지 방식으로 상세히 비교해 보겠습니다.

---

## 1. 📦 실생활 비유: 해외 직구와 택배 배송

`fetch`를 이용해 인터넷에서 데이터를 가져오는 과정은 **해외 직구**와 똑같습니다.

1. **`fetch(주소)` 호출:** 해외 쇼핑몰에 물건을 주문합니다.
2. **`Promise` 반환:** 물건이 당장 오지 않으므로, 쇼핑몰은 우리에게 **'배송 송장 번호(Promise)'**를 줍니다. 
3. **`.then()` (또는 `await`):** 택배가 무사히 도착하면, 상자를 열어 물건을 확인합니다.
4. **`.catch()` (또는 `catch` 블록):** 만약 세관에서 걸리거나 택배가 분실되면(에러 발생), 환불 처리를 합니다.

이제 우리가 주문할 해외 쇼핑몰 주소는 유저 목록을 무료로 제공해 주는 가짜 API 서버인 `https://jsonplaceholder.typicode.com/users` 입니다!

---

## 2. 전통적인 약속: `Promise`와 `.then()` 방식

가장 먼저 `Promise`의 원래 방식인 `.then()`을 사용하여 데이터를 가져오는 방법입니다.
택배가 도착하면 그 안의 물건(데이터)을 꺼내기 위해 보통 **두 번의 포장**을 뜯어야 합니다.

1. **첫 번째 `.then()`:** 택배 상자가 무사히 도착했는지(응답 상태) 확인하고, 상자 안의 내용물을 자바스크립트가 읽을 수 있는 형태(`JSON`)로 변환합니다.
2. **두 번째 `.then()`:** 변환이 완료된 진짜 데이터(유저 목록)를 꺼내서 화면에 보여줍니다.

```javascript
// Promise 방식으로 유저 데이터 가져오기
fetch('[https://jsonplaceholder.typicode.com/users](https://jsonplaceholder.typicode.com/users)')
  .then((response) => {
    // 1. 택배 도착! 상자를 뜯어서 내용물을 JSON(자바스크립트 객체) 형태로 변환해 줘!
    return response.json(); 
  })
  .then((users) => {
    // 2. 변환 완료! 이제 진짜 유저 목록 데이터를 쓸 수 있어!
    console.log("가져온 유저 수:", users.length);
    console.log("첫 번째 유저 이름:", users[0].name);
  })
  .catch((error) => {
    // 배송 중 문제 발생 시 (인터넷 끊김, 서버 터짐 등)
    console.log("데이터를 가져오는데 실패했습니다!", error);
  });
```

---

## 3. 현대인의 마법: `async / await` 방식

위의 `.then()` 방식도 좋지만, 함수 안에 또 함수(콜백)가 들어가 있어서 초보자가 한눈에 읽기엔 조금 낯설 수 있습니다. 
이를 일반적인 위에서 아래로 흐르는 코드처럼 만들어주는 것이 **`async/await`** 입니다.

```javascript
// 함수 앞에 async를 붙여서 "이 안에서 await를 쓸 거야!" 선언
async function fetchUserData() {
  try {
    // 1. fetch 앞에 await를 붙여서 택배가 올 때까지 일시정지!
    const response = await fetch('[https://jsonplaceholder.typicode.com/users](https://jsonplaceholder.typicode.com/users)');
    
    // 2. 도착한 택배를 JSON 형태로 변환할 때도 시간이 걸리므로 다시 await!
    const users = await response.json();
    
    // 3. 모든 과정이 끝나면 데이터 출력!
    console.log("가져온 유저 수:", users.length);
    console.log("첫 번째 유저 이름:", users[0].name);
    
  } catch (error) {
    // 4. try 안에서 문제가 생기면 즉시 이곳으로 넘어와서 에러 처리!
    console.log("데이터를 가져오는데 실패했습니다!", error);
  }
}
```
보시다시피 `async/await`를 사용하면 복잡한 들여쓰기 없이, **"데이터 가져와 -> JSON으로 바꿔 -> 출력해"** 라는 논리적인 순서가 그대로 코드에 드러납니다.

---

## 4. 🚨 초보자가 가장 흔히 하는 실수 (트러블슈팅 가이드)

인터넷에서 데이터를 가져올 때 입문자들이 99% 확률로 한 번씩 겪는 실수입니다.

### ❌ 실수 1: `json()` 변환할 때 `await` 깜빡하기
`fetch`로 데이터를 가져올 때만 `await`를 쓰고, `json()`으로 변환할 때는 깜빡하는 경우가 아주 많습니다.
* **실수:** `const users = response.json();` (❌ 에러! 변환이 끝나기도 전에 변수에 담아버림)
* **정답:** `const users = await response.json();` (⭕ 정답! 변환 작업도 비동기 작업이므로 반드시 `await`로 기다려야 합니다.)

### ❌ 실수 2: `async` 함수 안에서 `try...catch` 생략하기
데이터를 가져오는 작업은 내 컴퓨터가 아니라 **외부 인터넷 환경**에 의존합니다. 내 코드가 완벽해도 상대방 서버가 꺼져있거나, 내 와이파이가 끊기면 에러가 납니다.
따라서 `async/await`를 쓸 때는 무조건 **`try...catch`** 로 코드를 감싸서 프로그램이 강제 종료되는 것을 막는 습관을 들여야 합니다.

---

## 5. 🧠 배운 내용 확인하기 (미니 퀴즈)

오늘 배운 실전 데이터를 긁어오는 방법을 복습해 봅시다! (정답은 드래그하면 보입니다)

**Q1. 자바스크립트에서 외부 서버의 데이터를 가져오기 위해 사용하는 내장 함수의 이름은 무엇이며, 이 함수는 호출 시 무엇을 반환하나요?**
> 정답: <span style="color:transparent; text-shadow: 0 0 0 #000;">**fetch 함수 / Promise를 반환합니다.**</span>

**Q2. `fetch`로 가져온 응답(Response) 데이터를 자바스크립트에서 쉽게 사용할 수 있도록 객체 형태로 변환해 주는 메서드(함수)는 무엇인가요?**
> 정답: <span style="color:transparent; text-shadow: 0 0 0 #000;">**response.json()** (이 역시 비동기 작업이므로 await나 .then()이 필요합니다!)</span>