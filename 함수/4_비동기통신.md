# ⏳ 동기(Sync)와 비동기(Async): 웹 브라우저가 멈추지 않는 마법

자바스크립트가 일하는 방식을 이해하려면 **'동기(Synchronous)'**와 **'비동기(Asynchronous)'**라는 두 가지 개념을 반드시 알아야 합니다. 특히 웹 개발에서는 '비동기'가 필수적입니다. 과연 무엇이고 왜 필요할까요?

---

## 1. 동기(Sync) vs 비동기(Async) 란?

### 🧺 실생활 비유: 밀린 집안일 하기 (세탁기와 청소기)

주말을 맞아 밀린 집안일(세탁기 돌리기 1시간, 청소기 돌리기 30분)을 해야 한다고 상상해 봅시다.

**1) 동기(Synchronous) 방식: "하나 끝나면 다음 거 해!"**
* 세탁기에 빨래를 넣고 전원 버튼을 누릅니다.
* **(중요)** 세탁기가 다 돌아갈 때까지 1시간 동안 세탁기 앞을 떠나지 않고 멍하니 기다립니다.
* 1시간 뒤 세탁이 끝나면, 그제야 청소기를 돌리기 시작합니다.
* **총 소요 시간: 1시간 30분** (아주 비효율적이죠!)

**2) 비동기(Asynchronous) 방식: "오래 걸리는 건 맡겨두고 딴 거 먼저 해!"**
* 세탁기에 빨래를 넣고 전원 버튼을 누릅니다.
* **(중요)** 세탁기가 돌아가는 동안(기다리지 않고), 곧바로 거실로 나와 청소기를 돌립니다.
* 30분 뒤 청소가 끝납니다. 쉬고 있다가 세탁기에서 "띠리링~" 완료 알림음(콜백)이 울리면 그때 빨래를 널러 갑니다.
* **총 소요 시간: 1시간** (아주 효율적입니다!)

즉, **비동기 통신**이란 특정 작업(데이터 가져오기 등)이 시작된 후, 그 작업이 끝날 때까지 기다리지 않고 **곧바로 다음 코드를 실행하는 방식**을 말합니다.

---

## 2. 비동기 통신은 왜 필요한가요? (Why?)

가장 큰 이유는 **"사용자의 화면(웹 페이지)이 멈추는 것을 막기 위해서"** 입니다.

자바스크립트는 기본적으로 **'싱글 스레드(Single Thread)'** 언어입니다. 즉, 일을 처리하는 일꾼이 딱 1명뿐입니다.
만약 인터넷 쇼핑몰에 접속했는데, 상품 사진 100장을 서버에서 다운로드하는 작업을 '동기(Sync)' 방식으로 처리한다면 어떻게 될까요?

일꾼 1명이 사진 100장을 다 다운받을 때까지 다른 일(버튼 클릭하기, 스크롤 내리기 등)을 전혀 할 수 없게 됩니다. 사용자 입장에서는 **웹사이트가 렉이 걸린 것처럼 완전히 멈춰버린 것**으로 보이게 되죠. 

하지만 **'비동기(Async)'** 방식으로 처리하면, 일꾼이 서버에 "사진 100장 좀 다운받아줘~" 라고 요청만(세탁기 전원 누르기) 해놓고, 곧바로 다른 일(화면 스크롤, 버튼 클릭 반응 등)을 할 수 있습니다. 덕분에 우리는 끊김 없이 부드러운 웹 서핑을 할 수 있는 것입니다.

---

## 3. 자바스크립트에서 비동기는 언제 사용되나요?

자바스크립트에서 비동기 방식이 쓰이는 대표적인 3가지 상황입니다. (모두 '오래 걸리거나, 언제 일어날지 모르는 일'이라는 공통점이 있습니다.)

1. **서버와 통신할 때 (가장 중요 🌟)**
   * 앞서 배운 `fetch` 함수를 이용해 외부 API나 데이터베이스에서 회원 정보, 날씨 정보, 뉴스 기사 등을 가져올 때 사용합니다. (인터넷 환경에 따라 얼마나 걸릴지 모르기 때문입니다.)
   
2. **타이머(Timer)를 사용할 때**
   * `setTimeout` (n초 뒤에 실행해), `setInterval` (n초마다 반복해) 같은 타이머 함수들은 대표적인 비동기 함수입니다. 알람을 맞춰놓고 다른 코드를 먼저 실행합니다.

3. **이벤트(Event)를 기다릴 때**
   * 사용자가 마우스를 클릭할지, 키보드를 누를지는 언제 일어날지 알 수 없습니다. 그래서 "클릭하면 이 함수를 실행해 줘!" 라고 예약(비동기)해 둡니다.

---

## 4. 🚨 초보자가 가장 흔히 하는 실수 (트러블슈팅 가이드)

### ❌ 실수: 비동기 작업의 결과를 '동기'처럼 바로 쓰려고 할 때
초보자분들이 가장 많이 겪는 에러는, 비동기 작업이 **끝나기도 전에** 그 결과값을 사용하려고 하는 것입니다.

```javascript
let userData;

// 1. 서버에 유저 데이터 요청 (비동기: 세탁기 돌리기)
fetch('[https://jsonplaceholder.typicode.com/users](https://jsonplaceholder.typicode.com/users)')
  .then(response => response.json())
  .then(data => {
      userData = data; // 나중에 세탁 끝나면 여기에 담아!
  });

// 2. 데이터 출력 (청소기 돌리기)
console.log(userData); 
```

**결과: `undefined` (에러 발생!)**
이유: 자바스크립트는 `fetch`를 요청만 해놓고, 데이터가 도착하기도 전에 곧바로 아랫줄의 `console.log(userData)`를 실행해버립니다. 당연히 데이터가 아직 도착하지 않았으니 텅 비어있죠.
**해결책:** 앞서 배운 `await`를 붙여서 데이터가 올 때까지 기다리게 하거나, `.then()` 안에서 데이터를 출력해야 합니다!

---

## 5. 🧠 배운 내용 확인하기 (미니 퀴즈)

오늘 배운 핵심 개념을 복습해 봅시다! (정답은 드래그하면 보입니다)

**Q1. 이전 작업이 완전히 끝날 때까지 다음 작업을 시작하지 않고 멈춰서 기다리는 방식을 무엇이라고 부르나요?**
> 정답: <span style="color:transparent; text-shadow: 0 0 0 #000;">**동기 (Synchronous) 방식**</span>

**Q2. 자바스크립트에서 서버의 데이터를 가져오는 작업(`fetch`)을 동기 방식으로 처리하면 사용자에게 어떤 치명적인 문제가 발생할 수 있나요?**
> 정답: <span style="color:transparent; text-shadow: 0 0 0 #000;">**데이터를 가져오는 동안 브라우저(화면)가 멈춰버려 스크롤이나 클릭 등을 할 수 없게 됩니다.**</span>